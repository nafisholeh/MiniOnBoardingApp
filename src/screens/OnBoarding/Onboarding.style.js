import { StyleSheet } from "react-native";

export default StyleSheet.create({
  mask: {
    flex: 1,
    justifyContent: "flex-end",
    resizeMode: "stretch",
  },
  maskImages: {
    height: null,
    resizeMode: "center",
    width: null,
  },
});
